const initialState = {
  mapModalVisible: false,
  saveModalVisibility: false,
  aboutModalVisibility: false,
  parkInfoModalVisibility: false
};
export const modalsReducer = (state = initialState, { type, payload }) => {
  return state;
};
